---
interface Props {
  label: string;
  href: string;
}

const { label, href } = Astro.props;
---

<div>
  <a
    href={href}
    class="bg-primary hover:bg-gray-900 text-white font-bold py-2 px-4 max-w-sm flex flex-row justify-center items-center min-w-32"
    >{label}</a
  >
</div>

---
interface Props {
    label: string;
    href: string;
}

const {label, href} = Astro.props;
---
<div>
    <a href={href} class="bg-primary hover:bg-gray-900 text-white font-bold py-2 px-4 w-24 flex justify-center items-center">{label}</a>
</div>